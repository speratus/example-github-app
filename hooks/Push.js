import { Hook } from './Hook'

class Push extends Hook {
  constructor(payload, signature, type, delivery) {
    super(payload, signature, type, delivery)
    this.committer = payload.
  }
  
  committers() {
    authors = {}
    this.payload.commits.forEach((c) => {
      if (authors[c.])
    })
    return authors
  }
}

export default Push